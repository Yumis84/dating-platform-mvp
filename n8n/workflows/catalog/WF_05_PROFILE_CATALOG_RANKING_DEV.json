{
  "name": "WF_05_PROFILE_CATALOG_RANKING_DEV",
  "active": false,
  "settings": {"executionOrder": "v1"},
  "nodes": [
    {
      "id": "wf05-trigger",
      "name": "Catalog Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [0, 300],
      "parameters": {"httpMethod": "POST", "path": "dev-profile-catalog", "responseMode": "responseNode", "options": {}}
    },
    {
      "id": "wf05-input",
      "name": "Validate Catalog Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [220, 300],
      "parameters": {
        "jsCode": "const b=$json.body||$json; if(!b.user_id) throw new Error('user_id is required'); const limit=Math.min(Math.max(Number(b.limit)||25,1),50); const offset=Math.max(Number(b.offset)||0,0); return [{json:{viewer_user_id:String(b.user_id),limit,offset}}];"
      }
    },
    {
      "id": "wf05-query",
      "name": "Query Ranked WOMAN Catalog",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [440, 300],
      "credentials": {"postgres": {"id": "POSTGRES_PLACEHOLDER", "name": "POSTGRES_PLACEHOLDER"}},
      "parameters": {
        "operation": "executeQuery",
        "query": "WITH viewer AS (SELECT c.user_id,c.city_normalized,p.* FROM male_search_context c JOIN users u ON u.id=c.user_id LEFT JOIN male_search_preferences p USING(user_id) WHERE c.user_id=$1::uuid AND c.onboarding_status='COMPLETED' AND lower(u.role)='man'), candidates AS (SELECT profile.* FROM profiles profile JOIN users owner ON owner.id=profile.user_id CROSS JOIN viewer WHERE profile.status='ACTIVE' AND lower(owner.role)='woman' AND profile.city_normalized=viewer.city_normalized), scored AS (SELECT candidate.*,m.matched_preferences,m.considered_preferences,CASE WHEN m.considered_preferences=0 THEN 0::numeric ELSE m.matched_preferences::numeric/m.considered_preferences END match_score FROM candidates candidate CROSS JOIN viewer CROSS JOIN LATERAL (SELECT (CASE WHEN (viewer.age_from IS NOT NULL OR viewer.age_to IS NOT NULL) AND (viewer.age_from IS NULL OR candidate.age>=viewer.age_from) AND (viewer.age_to IS NULL OR candidate.age<=viewer.age_to) THEN 1 ELSE 0 END + CASE WHEN cardinality(viewer.districts)>0 AND candidate.district=ANY(viewer.districts) THEN 1 ELSE 0 END + CASE WHEN (viewer.height_from_cm IS NOT NULL OR viewer.height_to_cm IS NOT NULL) AND (viewer.height_from_cm IS NULL OR candidate.height_cm>=viewer.height_from_cm) AND (viewer.height_to_cm IS NULL OR candidate.height_cm<=viewer.height_to_cm) THEN 1 ELSE 0 END + CASE WHEN (viewer.weight_from_kg IS NOT NULL OR viewer.weight_to_kg IS NOT NULL) AND (viewer.weight_from_kg IS NULL OR candidate.weight_kg>=viewer.weight_from_kg) AND (viewer.weight_to_kg IS NULL OR candidate.weight_kg<=viewer.weight_to_kg) THEN 1 ELSE 0 END + CASE WHEN (viewer.breast_size_from IS NOT NULL OR viewer.breast_size_to IS NOT NULL) AND (viewer.breast_size_from IS NULL OR candidate.breast_size>=viewer.breast_size_from) AND (viewer.breast_size_to IS NULL OR candidate.breast_size<=viewer.breast_size_to) THEN 1 ELSE 0 END + CASE WHEN (viewer.price_from IS NOT NULL OR viewer.price_to IS NOT NULL) AND EXISTS(SELECT 1 FROM profile_prices price WHERE price.profile_id=candidate.id AND price.is_active AND (viewer.price_from IS NULL OR price.amount>=viewer.price_from) AND (viewer.price_to IS NULL OR price.amount<=viewer.price_to)) THEN 1 ELSE 0 END + CASE WHEN cardinality(viewer.meeting_place_types)>0 AND EXISTS(SELECT 1 FROM profile_meeting_places place WHERE place.profile_id=candidate.id AND place.is_active AND place.place_type=ANY(viewer.meeting_place_types)) THEN 1 ELSE 0 END)::int matched_preferences,(CASE WHEN viewer.age_from IS NOT NULL OR viewer.age_to IS NOT NULL THEN 1 ELSE 0 END + CASE WHEN cardinality(viewer.districts)>0 THEN 1 ELSE 0 END + CASE WHEN viewer.height_from_cm IS NOT NULL OR viewer.height_to_cm IS NOT NULL THEN 1 ELSE 0 END + CASE WHEN viewer.weight_from_kg IS NOT NULL OR viewer.weight_to_kg IS NOT NULL THEN 1 ELSE 0 END + CASE WHEN viewer.breast_size_from IS NOT NULL OR viewer.breast_size_to IS NOT NULL THEN 1 ELSE 0 END + CASE WHEN viewer.price_from IS NOT NULL OR viewer.price_to IS NOT NULL THEN 1 ELSE 0 END + CASE WHEN cardinality(viewer.meeting_place_types)>0 THEN 1 ELSE 0 END)::int considered_preferences) m), logged AS (INSERT INTO profile_search_events(user_id,filters) SELECT $1::uuid,jsonb_build_object('ranking','v1','city_hard_filter',true) WHERE EXISTS(SELECT 1 FROM viewer) RETURNING id) SELECT scored.id::text,scored.name,scored.age,scored.city,scored.district,scored.description,scored.matched_preferences,scored.considered_preferences,round(scored.match_score,4) match_score,count(*) OVER() total FROM scored ORDER BY match_score DESC,matched_preferences DESC,created_at DESC,id LIMIT $2::int OFFSET $3::int;",
        "additionalFields": {"queryParams": "viewer_user_id,limit,offset"}
      }
    },
    {
      "id": "wf05-response-shape",
      "name": "Prepare Catalog Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [660, 300],
      "parameters": {
        "jsCode": "const rows=$input.all().map(x=>x.json); const req=$('Validate Catalog Request').item.json; return [{json:{items:rows.map(({total,...item})=>item),total:rows.length?Number(rows[0].total):0,limit:req.limit,offset:req.offset}}];"
      }
    },
    {
      "id": "wf05-response",
      "name": "Respond Catalog",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [880, 300],
      "parameters": {"respondWith": "json", "responseBody": "={{$json}}", "options": {}}
    }
  ],
  "connections": {
    "Catalog Webhook": {"main": [[{"node": "Validate Catalog Request", "type": "main", "index": 0}]]},
    "Validate Catalog Request": {"main": [[{"node": "Query Ranked WOMAN Catalog", "type": "main", "index": 0}]]},
    "Query Ranked WOMAN Catalog": {"main": [[{"node": "Prepare Catalog Response", "type": "main", "index": 0}]]},
    "Prepare Catalog Response": {"main": [[{"node": "Respond Catalog", "type": "main", "index": 0}]]}
  },
  "meta": {"devOnly": true, "templateCredsSetupCompleted": false},
  "id": "WF_05_PROFILE_CATALOG_RANKING_DEV"
}
